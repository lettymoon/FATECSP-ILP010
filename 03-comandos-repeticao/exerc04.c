/*A potência de um número real x elevado a um número natural positivo n é igual ao produto de n
fatores iguais a x (por definição, todo número elevado a 0 é 1). Dados um número real x e um
número natural n, calcule e exiba a potência x^n.
*/

#include <stdio.h>

int main(void){

    int x, n;
    scanf("%d", &x);
    scanf("%d", &n);

    


    return 0;
}