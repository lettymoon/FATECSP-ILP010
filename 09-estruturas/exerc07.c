/*Exercício 07
Distância entre pontos no plano cartesiano
Complete o programa a seguir, de modo que ele funcione como está exemplificado na janela de execução ao lado.


INCOMPLETO*/

#include <stdio.h>

struct Ponto{
    int a;
    int b;
};

int le_ponto(struct Ponto){
    scanf()
}

int main(void){

    Ponto a = le_ponto(1);
    Ponto b = le_ponto(2);
    printf("Distancia: %.1f\n",distancia(a,b));

    return 0;
}